"use client";

export default function RadarChartClient() {
  return (
    <div className="rounded-[1.25rem] border border-dashed border-ink/20 bg-white p-6 text-sm leading-6 text-ink/65">
      Radar chart placeholder. This wrapper already uses dynamic import with `ssr: false`, so a future charting
      library can be plugged in later without changing the page structure.
    </div>
  );
}
